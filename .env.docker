# API
API_HOST=0.0.0.0
API_PORT=8000
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_MINUTES=60
REFRESH_EXPIRES_DAYS=7

# Database - Use service name 'postgres' inside Docker network
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=workflow_agent
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/workflow_agent

# Redis / Celery - Use service name 'redis' inside Docker network
REDIS_URL=redis://redis:6379/0

# Storage
STORAGE_PATH=./storage
