FROM node:18-alpine
WORKDIR /app

# Copy only package.json; package-lock.json is not committed.
COPY apps/web/package.json ./
RUN npm install || true

COPY apps/web ./apps/web
CMD ["npm", "run", "dev", "--", "--hostname", "0.0.0.0", "--port", "3000"]
