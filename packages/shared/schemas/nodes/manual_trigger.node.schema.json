{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "testPayload": {
      "type": "string",
      "description": "JSON test payload to use when manually triggering"
    },
    "allowDuplicates": {
      "type": "boolean",
      "default": true,
      "description": "Allow triggering multiple times with same payload"
    }
  }
}
